version: "3.9"

services:
  redis-benchmark:
    image: redis:7-alpine
    container_name: antdb-benchmark
    command: >-
      sh -c "redis-benchmark -h host.docker.internal -p 6379 -c 10 -n 1000 -t set,get,ping --threads 1"
    extra_hosts:
      - "host.docker.internal:host-gateway"
